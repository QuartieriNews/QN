APPROVED

1. **note — `code-nodes/parse-duration.js:69-70` (unchanged; outside PR #1).** The leading-zero rejection documented in `reviews/2026-08-30—cycle3—parse-duration—CHANGES_REQUESTED.md` remains an unresolved, separate parser finding. This PR changes only dependency declaration and installation guidance, so this approval does not close the parser review or approve publication. **Normative source:** `AGENTS.md`, Code Review Rules: “Review the diff since the last report in `reviews/`”; `README.md`, Pull request conventions: “One change per PR”. **Suggested fix:** resolve the parser finding in its own follow-up and review cycle; do not expand this dependency-only PR to include it.

Checks performed and found clean

- Reviewed [PR #1 — Declare openpyxl as a Python dependency](https://github.com/QuartieriNews/QN/pull/1), head `085b218c5c6f8c032f9f2c1af7b840af7dbd2e8d`, against base `d7f9b19d1ef7590a8f6dee3f33366e76d14dd813` (`main`). The raw Git diff contains exactly `requirements.txt`, `README.md` and `AGENTS.md`: 17 additions, one deletion. No new blocking finding or defect was found in that diff.
- Read the local routing and review mandate, the PR's agent rules and mandate pointer, README, `docs/START_HERE.md`, all decision records, the dependency declaration, the affected import chain, prior review context and the relevant executable-test criteria in specification v2.5 from the additional Drive folder. Existing geographic/editorial rules and phase contracts are unchanged by this PR.
- Confirmed that `gazetteer/build_gazetteer.py` imports `openpyxl` at module load, and that `tests/test_guards.py` imports its `norm` function. The other Python imports in the repository are standard-library or repository-local modules; the declared direct dependency covers the inspected import chain.
- Created a clean Python 3.12.13 virtual environment outside the repository, without inherited site packages. Verified that `openpyxl` was absent and that importing `norm` initially raised `ModuleNotFoundError: No module named 'openpyxl'`, reproducing the dependency problem independently.
- Installed only the PR's `requirements.txt` into that environment with `python -m pip install -r requirements.txt`: exit code 0, resolving `openpyxl 3.1.5` and its transitive dependency `et-xmlfile 2.0.0`. `python -m pip check` reported no broken requirements; importing the build's `norm` function then succeeded. No global Python environment was changed.
- From the exact PR snapshot's `gazetteer/`, `python ../tests/test_guards.py`: exit code 0, `ALL PASS (37 checks)`, including the normalization block that previously required the undeclared dependency. The previously recorded specification count drift is not introduced by this PR.
- `node --check code-nodes/parse-duration.js`: exit code 0. `node tests/test_parse_duration.js`: exit code 0, `ALL PASS (46 checks)`, including T39 and the committed adapter/overflow coverage. Passing that existing suite does not supersede the separate cycle-3 finding above.
- Ran `python build_gazetteer.py` only in the disposable PR snapshot: exit code 0; 440 neighbourhoods, 91 zones, 89 publication pages and 29 non-published neighbourhoods. The master workbook, `it_strings.json`, regenerated `gazetteer.json` and regenerated prompt list retained identical SHA-256 hashes. The generated Italian workbook could be reopened successfully with `openpyxl`.
- Verified snapshot files against the PR's Git blob hashes for both changed documentation files, `requirements.txt`, the build script and both test suites. `git diff --check d7f9b19 085b218`: exit code 0. The dependency file introduces no alternate package index, credential, executable install hook or unrelated direct dependency.
- Scope limits: this is the first review of PR #1, not cycle 4 of parse-duration. T45 and live n8n execution were not rerun: no prompt, workflow or publication behavior changed. Generated files were written only by the build test inside the temporary snapshot, never in the project checkout.
- Local `main` remains at `167972cb6dca0e6e0b12a87a501293700c9eb046`, two commits behind fetched `origin/main`; the PR branch was reviewed from an isolated archive without pulling or switching branches. No production code, existing report, index or PR was modified. No commit, push, merge or GitHub review submission was performed; this local reviewer report is the only project file created.
