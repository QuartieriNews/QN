# Review Mandate — Code

**Moved.** Since the migration to agent-native tooling, the review mandate lives
in `/AGENTS.md` (section "Code Review Rules"), where Codex and any other
reviewing agent read it automatically. This file remains only as a pointer and
as the paste-source for chat-based review sessions: if reviewing from a plain
chat, paste the whole of `/AGENTS.md` instead.

One rule of this repository applies to instructions too: every rule lives in
exactly one place. Do not restate review rules here.
